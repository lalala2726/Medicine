<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.medicine.agent.mapper.MallOrderItemMapper">

    <select id="getProductSalesByIds" resultType="cn.zhangchuangla.medicine.agent.model.dto.ProductSalesDto">
        SELECT moi.product_id AS productId,
        SUM(moi.quantity) AS sales
        FROM mall_order_item moi
        INNER JOIN mall_order mo ON moi.order_id = mo.id
        WHERE moi.is_deleted = 0
        AND mo.is_deleted = 0
        AND mo.order_status = 'COMPLETED'
        AND moi.product_id IN
        <foreach collection="productIds" item="productId" open="(" separator="," close=")">
            #{productId}
        </foreach>
        GROUP BY moi.product_id
    </select>
</mapper>
