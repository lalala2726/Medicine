spring:
  ai:
    openai:
      # 这边不要删除，这是占位符,数据库模型信息会从数据库进行读取,如果删除了会导致系统启动失败
      api-key: _

logging:
  level:
    org.springframework.ai: DEBUG
    org.springframework.ai.chat.client: DEBUG
    # Add other specific packages if needed
    org.springframework.ai.openai: DEBUG
