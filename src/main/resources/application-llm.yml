spring:
  ai:
    vectorstore:
      milvus:
        client:
          host: localhost
          port: 19530
          password: milvus
          username: root
        collection-name: medicine_embeddings
        index-type: HNSW
        metric-type: COSINE
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com/
      chat:
        options:
          model: deepseek-chat
