<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.medicine.admin.mapper.LLMConfigMapper">

    <resultMap id="BaseResultMap" type="cn.zhangchuangla.medicine.model.entity.LlmConfig">
        <id property="id" column="id"/>
        <result property="provider" column="provider"/>
        <result property="model" column="model"/>
        <result property="apiKey" column="api_key"/>
        <result property="baseUrl" column="base_url"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="deleteTime" column="delete_time"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,provider,model,api_key,base_url,status,
        create_time,update_time,
        create_by,update_by,delete_time,is_delete
    </sql>

    <select id="listLlmConfig" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM llm_config
        <where>
            is_delete = 0
            <if test="request.id != null">
                AND id = #{request.id}
            </if>
            <if test="request.provider != null and request.provider != ''">
                AND provider LIKE CONCAT('%', #{request.provider}, '%')
            </if>
            <if test="request.model != null and request.model != ''">
                AND model LIKE CONCAT('%', #{request.model}, '%')
            </if>
            <if test="request.status != null">
                AND status = #{request.status}
            </if>
            <if test="request.createBy != null and request.createBy != ''">
                AND create_by = #{request.createBy}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper>
