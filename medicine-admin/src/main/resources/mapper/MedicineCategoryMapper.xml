<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.medicine.mapper.MedicineCategoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.zhangchuangla.medicine.model.entity.MedicineCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="description" column="description"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="deleteTime" column="delete_time"/>
        <result property="isDelete" column="is_delete"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, parent_id, description, sort, create_time, update_time, create_by, update_by, delete_time, is_delete
    </sql>

    <!-- 分页查询药品分类列表 -->
    <select id="listMedicineCategory" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM medicine_category
        <where>
            is_delete = 0
            <if test="request.id != null">
                AND id = #{request.id}
            </if>
            <if test="request.name != null and request.name != ''">
                AND name LIKE CONCAT('%', #{request.name}, '%')
            </if>
            <if test="request.parentId != null">
                AND parent_id = #{request.parentId}
            </if>
            <if test="request.description != null and request.description != ''">
                AND description LIKE CONCAT('%', #{request.description}, '%')
            </if>
        </where>
        ORDER BY sort ASC, create_time DESC
    </select>

</mapper>
